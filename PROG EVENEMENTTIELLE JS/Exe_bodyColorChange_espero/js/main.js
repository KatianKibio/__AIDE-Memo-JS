//ICI c'est l'écriture simplifier
// on aurai dû utiliser adEventListener
window.onload = () => {
    setupListener()
}

//document.addEventListener('click' listenerFunction) est = à ↓
/* → */ //document.onclick = listernerFunction